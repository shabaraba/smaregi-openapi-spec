type: object
description: 値引き区分情報
properties:
  id:
    type: integer
    description: 値引き区分ID
  divisionName:
    type: string
    description: 値引き区分名
  code:
    type: string
    description: 値引き区分コード
  displayOrder:
    type: integer
    description: 表示順
  createDate:
    type: string
    format: date-time
    description: 作成日時
  updateDate:
    type: string
    format: date-time
    description: 更新日時
required:
  - id
  - divisionName
